# docker hub repository credentials
# docker images to get the tag
docker tag ? accountname/docker-whale:latest
docker login --username=user --email=my_email
# publish to repo 
docker push accountname/docker-whale
